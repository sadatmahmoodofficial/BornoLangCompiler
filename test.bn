ধরি সংখ্যা ক = ১০;
ধরি সংখ্যা খ = ৫ * (২ + ৩);
ধরি সত্যমিথ্যা ফলাফল = সত্য;

যদি (ক < খ) {
    দেখাও(খ);
    যদি (ফলাফল == সত্য) {
        দেখাও(১০০);
    }
} অথবা {
    দেখাও(০);
}

যতক্ষণ (ক < ২০) {
    ক = ক + ১;
    দেখাও(ক);
}