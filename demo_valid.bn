ধরি সংখ্যা ক = ১০;
ধরি সংখ্যা খ = ২০;
ধরি সত্যমিথ্যা পতাকা = সত্য;

যদি (পতাকা == সত্য) {
    যদি (ক < খ) {
        দেখাও(ক * ২ + খ);
    } অথবা {
        দেখাও(০);
    }
}

যতক্ষণ (ক < ১৫) {
    ক = ক + ১;
    দেখাও(ক);
}